# Copy to .env and set your token from Discord Developer Portal
DISCORD_TOKEN=your_discord_bot_token_here

# Optional: path to libopus if "Error occurred while decoding opus frame" or voice fails (macOS: brew install opus; Linux: libopus0)
# OPUS_LIB_PATH=/opt/homebrew/lib/libopus.dylib

# Optional: logging level (DEBUG, INFO, WARNING, ERROR) and log file path
# LOG_LEVEL=INFO
# LOG_FILE=watson.log

# Optional: directory for temp WAV and transcript files (default: system temp/watson)
# WATSON_TEMP_DIR=/tmp/watson

# Optional: max recording length in minutes (default 30); recording stops after this, use !record again for a new segment
# RECORDING_MAX_MINUTES=30

# Optional: chunked recording — block duration in seconds (default 300 = 5 min)
# BLOCK_DURATION_SECONDS=300

# Optional: Whisper transcription — language code (e.g. ru, en) and beam_size (default 5)
# Audio is converted to 16 kHz mono 16-bit before save and before transcribe.
# TRANSCRIPT_LANGUAGE=ru
# TRANSCRIPT_BEAM_SIZE=5

# Optional: directory for saved transcripts and WAV (default: recordings/ in project root)
# Relative paths are resolved from project root; absolute path used as is.
# WATSON_RECORDINGS_DIR=recordings
